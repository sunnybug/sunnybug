先来几组PC-Lint的相关数字：
1、1000:检查上千种编码规范错误
2、328:每种编码错误都有详细说明，说明文件（txt格式）达到328KB，可作为编码规范教材使用。
3、367:帮助文档多达367页
4、1985：1985年开始研发

可检查到错误包括但不局限于：
	可展开宏之后检查错误
	变量未初始化或赋值却被调用
	变量/符号/枚举未被使用
	报告冗余代码(宏定义、typedef、声明、结构、联合和枚举类型等有定义但未使用、头文件重复包含、头文件被包含但是未使用）
	类成员的初始化顺序和声明顺序不一致
	精度缺失
	数组/函数（如memcpy）缓冲区溢出
	switch/case检查（类型是否匹配，是否有default，是否有未case的枚举值）
	空指针/除0检查
	部分内存泄露

根据以下条件关闭/开启指定的警告：
	1、函数
	3、警告级别
	4、宏
	5、符号名（类名、变量名等）
	6、某一代码行

自定义函数检查：
	如LogSave(const char* fmt, ...)，需要检查格式类型是否匹配，则可通过以下配置实现：
	-printf(1, LogSave)

变量值跟踪


和VS2008的代码检查功能比较：
1、vs2008无法检查自定义函数的printf
2、传参检查